@model IEnumerable<Repuestos2023.Models.Models.Proveedor>
@{
    ViewData["Title"] = "Index";
}

<div class="container">
    <div class="row w-75">
        <div class="col-6">
            <h2>Suppliers List</h2>

        </div>
        <div class="col-6 text-end">
            <a asp-controller="Proveedor" asp-action="Create" class="btn btn-primary">
                <i class="bi bi-plus-circle"></i> &nbsp;
                New Supplier
            </a>
        </div>
    </div>
    <table class="table table-bordered table-striped w-75">
        <thead>
            <tr>
                <th style="width:50%">Proveedor Name</th>
                <th style="width:25%">Actions</th>
            </tr>
        </thead>
        <tbody>
            @if (Model.Any())
            {
                foreach (var item in Model)
                {
                    <tr>
                        <td>@item.NombreProveedor</td>
                        <td>
                            <a class="btn btn-warning" asp-controller="Proveedor" asp-action="Edit" asp-route-id="@item.ProveedorId">
                                <i class="bi bi-pencil-square"></i>&nbsp;
                                Edit
                            </a>
                            <a class="btn btn-danger" asp-controller="Proveedor" asp-action="Delete" asp-route-id="@item.ProveedorId">
                                <i class="bi bi-trash3"></i> &nbsp;
                                Delete
                            </a>

                        </td>
                    </tr>
                }
            }
            else
            {
                <tr>
                    <td colspan="3">
                        No suppliers added yet
                    </td>
                </tr>
            }
        </tbody>
    </table>

</div>

